# 时令风物：3D 建模与接入计划

规划日期：2026-09-26。状态：佛手瓜苗首款样板已接入本地页面；八月瓜尚未开始，真实手机仍需实机复核。

目标：用真实参考驱动的程序化建模，做出可转动、可点开吃法的立体食材。首批交付佛手瓜苗和八月瓜两个样板；是否扩充由样板的形态、交互和手机表现决定。

## 1. 已核对的基础与本次假设

- 现有站点是 `dist/` 下的原生 HTML、CSS、JavaScript 静态页面。风味星图目前由 Canvas 2D 绘制，没有现成 Three.js 播放器。
- 食材详情已有 `openFood(id)`、分享参数 `?food=...`、照片、做法和 B 站入口，3D 接入复用这些内容。
- 采用自然植物图鉴式立体插画：重视轮廓、连接、食用状态与颜色；画面使用柔和光照、简洁背景和适度表面细节。单图背面补全不能视为扫描还原。
- 第一个建模对象是单根带叶、带卷须的佛手瓜嫩梢；它通过后，再组成有疏密关系的小簇。第二个对象是八月瓜整果。南湖菱列入后续候选，先锁定具体品种与整果、果肉参考。
- 首款样板直接按参考图编写 Three.js 几何，没有运行 img2threejs 生成器；也没有获得参考对话中提到的 ZIP 执行包。

## 2. 首批模型与参考缺口

| 样板 | 第一版造型 | 用来验证 | 现有依据与下一步 |
| --- | --- | --- | --- |
| 佛手瓜苗 `foshougua-miao` | 单根完整嫩梢；结构通过后形成小簇 | 弯曲叶片、细茎、卷须、部件连接和多角度遮挡 | 已查看 `assets/chayote-raw.jpg`，其采收场景适合颜色及整体状态参考，单根连接关系被遮挡；补单梢正面、侧面及叶柄/卷须连接处照片 |
| 八月瓜 `bayuegua` | 一枚完整果实，保留参考中的不对称轮廓与果柄 | 封闭曲面、体积、果皮颜色与粗糙度 | 已查看 `assets/bayuegua.webp`；先按该参考的整果状态建模。成熟裂口与内部果肉另找同种、相应成熟状态资料后再规划 |

准备工作先查现有候选图库及公开植物资料。建模参考记录照片来源、物种或品种、成熟/采收状态、许可、视角与关键特征。现有 `IMAGE_REGISTER.md` 可作为入口，不能直接把其中所有候选当作已通过身份核验的素材。

## 3. 每款的制作顺序

1. **参考与结构说明。** 选定一张主参考，补必要视角。写出轮廓比例、叶缘/果皮等辨识特征、各部件连接、可见细节和照片未覆盖的区域。后者写在制作记录中；页面使用简短的“立体插画”标识即可。
2. **灰模。** 先做无复杂材质的完整三维形体。嫩茎、叶柄、卷须使用曲线管状几何；叶片使用有弯曲、有正反面的网格；果实按实际轮廓建立不对称曲面。检查侧面厚度和体积。
3. **连接与形态。** 沿主茎逐段核对叶柄和卷须的起点，修正穿插、悬空和完全重复的叶片姿态。通过这一关再加装饰细节。
4. **材质与灯光。** 校对颜色、叶脉/果皮细节、粗糙度及光泽；采用固定、柔和的灯光。表面噪声必须使用固定随机种子，重载不会变成另一株。首版只需要整体观察旋转，无需植物摆动骨骼。
5. **四向复核。** 保存正面、两侧、背面和俯视截图，正面对照参考；有实拍的方向核对形态，没有实拍的方向检查空间结构是否合理。加入一张实际手机版面的构图截图。
6. **接入与性能。** 完成模型选择、拖动、详情打开、照片回退和手机手势后才形成可发布资产。

每次返修只处理一个明确问题，保留上个较好版本。采用每阶段最多三轮、单款最多六轮纠正作为首批工作预算；达到上限就提交可转动的最佳版本和具体缺陷，决定补图或缩小造型范围，不以材质润色掩盖形态问题。此预算参照当前上游流程，执行时记录所锁定版本的实际规则。

## 4. 先把单款交互做通

| 使用情境 | 行为与验收 |
| --- | --- |
| 桌面浏览 | 首次进入可轻转约六秒后停止；用户操作立即停止自动转动。减少动态效果偏好开启时不自动播放 |
| 桌面拖动 | 按住拖动观察，释放后短暂惯性；指针捕获保证拖出画布仍能正确结束。禁止平移出画面 |
| 样板/首页单击 | 命中食材并满足轻点条件后，打开对应的现有详情，不弹演示对话框 |
| 滚轮 | 默认滚动网页；主动进入“转一转”后，画布内才允许有限缩放。鼠标离开画布后正常滚动网页 |
| 手机浏览 | 上下滑动滚页面、双指可缩放页面，轻点食材打开详情 |
| 手机观察 | 先点击独立的“转一转”，下一次手势才用于旋转；“退出观察”和“复位”始终可用 |
| 详情阅读 | 可切换立体观察与实物照片；“看吃法”定位到现有做法。退出详情恢复原节气、滚动位置和观察角度 |
| 加载失败 | 首帧成功前保留照片；脚本失败、WebGL 不可用或图形上下文丢失时回到照片，详情按钮继续可用 |

点击与拖动由手势状态判断：按下记录位置、对象和时间，移动记录全程最大位移；起步阈值为 8 CSS 像素。超过阈值、出现多指、`pointercancel`、丢失指针捕获或窗口失焦，均取消点击资格。拖出后再回到起点也不能触发详情。保留普通 HTML 的“查看吃法”按钮和键盘操作入口。

OrbitControls 负责相机环绕与阻尼，Raycaster 负责食材命中，业务回调负责打开现有详情。自动转动与手动环绕使用同一控制器，避免同时旋转模型根节点和相机。模型朝向只用于初始构图。

手机浏览状态使用 `touch-action: pan-y pinch-zoom`；观察状态才使用 `touch-action: none`。进入浏览状态时必须确认 OrbitControls 没有仍在接管触摸，不能只设置 `enabled=false` 就假定其 CSS 和监听器均已恢复。模式切换在下一次手势开始前完成。

## 5. 网站接入范围

第一阶段在本地独立样板页中验证一款的完整模型与交互，再接入佛手瓜苗详情；模型和版面均通过后，在首页增加一个当季立体展示位置。首页仍显示所有当季食材，只有已完成且符合所选时令的模型进入展示位；未覆盖的节气自然显示照片。

立体食材在专用留白里构图，与标题保持间距。小图标继续在名字旁，不叠在照片或模型主体上。详情保留大幅实物照片供识别。

实现使用原生 JavaScript ES modules 与固定版本的 Three.js。主库、OrbitControls 和所需资源随网站提供，通过相对路径加载并验证 GitHub Pages 的项目子路径。首版采用本地模块和 import map 即可，不引入 React 或重写地图、风味星图。

代码分工控制在三层：

- **单款模型工厂：** 返回模型根对象、可点击对象和释放资源的方法。它不创建画布、不绑定手势、不启动独立动画循环。
- **共用展示器：** 统一管理一个活跃画布、相机、光照、观察状态、命中判断、资源清理和照片回退。切换食材时保存相机状态，尺寸变化时按模型边界重新适配。
- **现有页面连接：** 根据食材 ID 懒加载模型；调用 `openFood(id)` 和现有详情内容；复用既有分享链接。

制作来源与复核结果保存在独立记录中，不打包成必须展示给食客的长篇说明。先交付程序化模型源码；GLB 导出仅在后续出现跨工具复用需求时增加。

## 6. 起步性能预算

以下为首批设计目标，尚未实测：

- 同时只有一个活跃 3D 画布；其余位置显示照片或模型预览图。
- 单款先控制在约 2 万三角面、50 次绘制调用以内；手机像素比上限先设为 1.5。
- 单款代码和纹理新增传输量目标不超过 1.5 MB，另列共享 Three.js 运行库的实际传输量。
- 桌面拖动以接近 60 FPS、目标手机以稳定 30 FPS 为起步目标；记录设备、浏览器、模型生成耗时和卡顿情况，不能以面数代替实机结果。
- 模型进入视口或被主动打开时加载；离开视口、详情遮挡原展示、标签页隐藏时暂停。自动转动及阻尼结束后停止连续渲染。
- 切换或销毁模型时移除监听器、停止循环，释放自有几何、材质和纹理；重复打开关闭十次，确认资源使用没有持续累积。

## 7. 分阶段交付与通过条件

| 阶段 | 交付 | 通过条件 |
| --- | --- | --- |
| A 参考准备 | 两款参考包、辨识特征表、缺口表、锁定版本记录 | 佛手瓜苗至少有可辨认的单根结构；八月瓜整果的物种和状态明确 |
| B 交互底座 | 本地样板页、通用测试物、照片回退 | 转动、点击开详情、拖动不误触、退出观察均有效；测试物不计为食材模型成果 |
| C 佛手瓜苗 | 单梢灰模、成品模型、四向及俯视截图、对照记录 | 辨识特征和连接正确，侧背面成立；交付可亲手转动的版本供视觉评估 |
| D 实际页面接入 | 详情页样板，随后是首页构图 | 原节气、全部当季列表、地图交互、详情分享、关闭回位均可用 |
| E 八月瓜 | 整果模型、同套交互、对照记录 | 增加第二款无需另写旋转/点击逻辑；体积、颜色、果柄与参考一致 |
| F 发布准备 | 交互与性能记录、来源记录、回退版本 | 桌面与可用的真实手机验证通过；只有模拟测试时明确记录实机未测，不宣称实机通过 |

关键回归情景：小幅点击、长距离拖动后返回起点、多指及取消手势、拖出画布松手、画布外滚轮、手机普通上下滑动、网络失败、关闭后恢复位置、分享链接直达。针对这些行为进行检查，不为静态素材堆砌测试。

首个里程碑是一款可转动、可点开正确详情的佛手瓜苗，加上参考对照和手机版面。通过后再完成八月瓜和正式站点接入。暂不估算 24 款总工期；用首款实际的搜图、返修和性能记录估计后续成本。

## 8. 依据与使用边界

- [img2threejs 官方技能手册](https://github.com/img2threejs/img2threejs/blob/main/SKILL.md)：参考图驱动的程序化建模、分阶段检查与有限返修。当前读取版本标识为 2.0.0；开工时锁定具体提交。借鉴其流程不等于已经运行完整上游流水线，也不证明所有植物均能达到所需精度。
- [img2threejs README](https://github.com/img2threejs/img2threejs/blob/main/README.md)：安装和多宿主使用方式。规划阶段无需安装；实际使用时再核对依赖与适用流程。
- [Three.js OrbitControls](https://threejs.org/docs/pages/OrbitControls.html)：环绕、阻尼、缩放限制与自动旋转。
- [Three.js Raycaster](https://threejs.org/docs/pages/Raycaster.html)：三维对象命中判断。
- [MDN touch-action](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/touch-action)：浏览器滚动与自定义手势的边界；手势开始后修改此属性不会改变该次手势。

规划同时参考了所关联对话的完整流程，实施范围以本项目现有代码和本文件的首批交付为准。

## 9. 佛手瓜苗样板执行记录

- Three.js 与 OrbitControls 固定在 0.186.1，随 `dist/vendor/three/` 提供。模型工厂在 `dist/food3d/chayote.js`，共用展示器在 `dist/food3d/viewer.js`。
- 正面及食用状态参考：项目已有的越南嫩梢采收照；叶形和卷须参考：[Pl@ntUse 的物种描述](https://plantuse.plantnet.org/en/Sechium_edule_%28PROSEA%29)、[World Flora Online](https://www.worldfloraonline.org/taxon/wfo-0000441843)、[Wikimedia 叶与卷须照片](https://commons.wikimedia.org/wiki/File:Green_leaves_of_Chayote.jpg)。后者为同种植物形态参考，拍摄地与采收状态不能替代越南嫩梢照片。
- 目前做出一根嫩梢，包含弯曲主茎、叶柄、五片不同大小和朝向的曲面叶、正反叶脉与从茎节伸出的卷须。不可见背面的姿态按植物结构补全；它是立体插画，不能作为采食鉴定依据。
- 已在浏览器核对桌面拖动、模型轻点、详情定位、手机模拟触摸滚动和观察模式，以及 WebGL 上下文丢失时显示本地照片。页面只有一个活跃画布；本机样板 9248 个三角面、26 次绘制调用。真实手机流畅度与手感尚未实测。
